const Navbar = () => {
    // Creates navbar with HTML with anchor tags for links.
    return ( 
        <nav className="navbar p-3">
            <div className="container-fluid">
                <h2 className="title fw-bold h1 text-light">Resume Buddy</h2>
                
            </div>
         </nav>
        );
}
 
export default Navbar;